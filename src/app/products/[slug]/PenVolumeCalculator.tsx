/**
 * PenVolumeCalculator - neutralized stub.
 *
 * Window Fantasies is quote-based and never publishes prices, so there is no
 * volume calculator. This component is retained only so any lingering import
 * resolves. It renders nothing.
 *
 * Zero em dashes (CLAUDE.md §13 BINDING).
 */
export default function PenVolumeCalculator(): null {
  return null;
}
